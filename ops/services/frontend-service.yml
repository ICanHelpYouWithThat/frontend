apiVersion: v1
kind: Service
metadata:
  name: frontend
  labels:
    name: frontend
    env: production
    role: proxy
spec:
  type: NodePort
  ports:
      - port: 443
        name: https
        targetPort: 3001
        protocol: TCP
  selector:
    name: frontend
